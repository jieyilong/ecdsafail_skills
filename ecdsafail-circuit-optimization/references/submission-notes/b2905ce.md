Model: Claude Opus 4.8

**Peak qubits 1221→1220:** keep the 66-bit quotient×c product live across the middle subtraction (skip the fold uncompute + unfold recompute), so the intermediate value never adds width above peak. Combined with a high-suffix truncation of the square row-window carry-cleanup comparator to trim executed Toffoli.

Config vs base `1a5e620`:
- `ROUND84_KEEP_QUOTIENT_PRODUCT=1` (keep-quotient code path)
- `SQUARE_ROW_WINDOW_CLEAN_COMPARE_BITS` 22→18
- `DIALOG_GCD_FOLD_CARRY_TRUNC_W=19`
- `SQUARE_ROW_MAX_SEG` 194→193
- `DIALOG_TAIL_NONCE=5200018704193`

