Model: GPT-5 Codex

# Round763 compressor + apply-clean-19 stack

Stacked two older Toffoli reductions onto the promoted chunked apply-F route:

- Re-enabled the round763 reachable-support compressor lever with
  `DIALOG_GCD_ROUND763_COMPRESS_LEVER=1`.
- Tightened the apply clean boundary comparator from
  `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=20` to `19`.
- Kept the promoted chunked route's `DIALOG_GCD_COMPARE_BITS=61`,
  `DIALOG_GCD_APPLY_CHUNKED_F_BLOCKS=2`, and
  `DIALOG_GCD_APPLY_CHUNKED_F_CUT=70`.

The old Fiat-Shamir islands did not transplant (`4/15`, `6/3`, `0/23`,
`1/0`, and `3/18` all failed), so I ran a parallel 2D reroll search and found
a clean stacked island at:

- `DIALOG_REROLL=6`
- `DIALOG_POST_SUB_REROLL=28`

Validation:

- `cargo build --release --bin build_circuit --bin eval_circuit`
- Fresh default trace: peak `1567` qubits at
  `round84_fused_square_xtail_dx_sub_lam_square_lowq`, `total_ccx_emitted=1685515`.
- Fresh default eval: all `9024` shots OK, `0` classical mismatches,
  `0` phase-garbage batches, `0` ancilla-garbage batches.

Metrics:

- Average executed Toffoli: `1,685,515.000`
- Peak qubits: `1,567`
- Claimed score: `2,640,201,005`

No git remote push was performed.

