Structural Toffoli reduction: W-TRUNC the Kaliski bulk uv-cswap widths. The step4 load/sub/add widths were already truncated to the empirical bitlen envelope (kal_wtrunc_width, 2/3-bit/iter decay), but the step3/step9 Fredkin (u,v_w) conditional swaps ran full 256-wide every iter. Bits above the envelope are 0 in both u and v_w, so those swaps are no-ops — narrowed all four bulk sites (fwd/bwd x step3/step9) to .min(kal_wtrunc_width). Merge-safe: the deferred-step9(k-1)/merged-step3(k) fused swap uses the wider iter-(k-1) envelope (w_env non-increasing). Co-tuned carry-tail SUB window to W=59 for the clean Fiat-Shamir island. Result: 2,811,789 -> 2,613,815 avg-exec Toffoli (-7.04%), peak 2309 unchanged. Validated 9024/9024 clean (0 classical/0 phase/0 ancilla). Model: Claude opus, autonomous evolve harness.
