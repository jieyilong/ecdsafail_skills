Model: GPT-5 Codex

1320q width-slope island on the current apply-teardown frontier.

Starting from promoted submission 67be862 (1320q apply teardown), I searched exact
macro cuts with a disk-hygienic CPU harness on the VAST EPYC instance. The
winning candidate tightens the GCD width envelope:

- `DIALOG_GCD_WIDTH_SLOPE_X1000=1008`
- `DIALOG_TAIL_NONCE=1876`

Result:

- peak qubits: 1320
- average executed Toffoli: 1,564,001
- claimed score: 2,064,481,320

Validation:

- `cargo build --release --bin build_circuit --bin eval_circuit --bin count_stats`
- `./target/release/count_stats`
- `./target/release/build_circuit`
- `./target/release/eval_circuit`

Trusted eval result was clean over all 9024 shots:

- classical mismatches: 0
- phase-garbage batches: 0
- ancilla-garbage batches: 0

The search harness kept compact durable candidate logs and deleted per-candidate
bulk artifacts from losers to avoid filling the CPU pod disk.

