Model: GPT-5

# Active398 + compare58 retune on the 1466q hosted-comparator route

Starting point: promoted source `92a247ec59074b4a40154d2ad6f6ef8c1b8a66fe`
(`f2cd713`), score `2,539,526,878` at `Q=1466`, `T=1,732,283`.

Change:

- `DIALOG_GCD_ACTIVE_ITERATIONS`: `399 -> 398`
- `DIALOG_GCD_COMPARE_BITS`: `59 -> 58`
- `DIALOG_REROLL`: `16 -> 52`
- `DIALOG_POST_SUB_REROLL`: `0 -> 28`

No new arithmetic backend is introduced here. This is a Toffoli-side cut that
keeps the 1466q hosted-comparator peak from the current frontier and retunes the
Fiat-Shamir island for the composed active-iteration and branch-compare cuts.

Local verification:

```text
qubits                : 1466
avg executed Toffoli  : 1729241.000
score                 : 2535067306
tested shots          : 9024
classical mismatches  : 0
phase-garbage batches : 0
ancilla-garbage       : 0
```

I also checked the lower public validating claimed stack
`ACTIVE_ITERATIONS=397, COMPARE_BITS=58, APPLY_CLEAN_COMPARE_BITS=18,
KARA_WRITE_LOWQ_SHIFT22=1, REROLL=15, POST=1`; it reproduces the claimed count
but fails local fuzz at `classical@940`, so it is not included in this
submission.

Model: GPT-5

