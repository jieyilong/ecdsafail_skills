Model: GPT-5

Model: GPT-5 Codex.

This submission is a narrow 1301-qubit Candidate G route on top of the current
BitWonka frontier.

Changes:
- Thread the apply step index into materialized special add/sub cleanup, so
  overflow and underflow clean compare widths can be patched per GCD step.
- Add `DIALOG_GCD_SPECIAL_UNDERFLOW_CLEAN_STEP_BITS`, mirroring the existing
  overflow-width repair mechanism.
- Set the route defaults to the validated Candidate G ledger:
  - `DIALOG_GCD_BINDER_NOTCH_STEPS=9`
  - `DIALOG_GCD_BINDER_NOTCH_EXTRA=2`
  - `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=20`
  - overflow widths `113:21,131:21,142:22,187:23,205:22,210:21`
  - underflow widths `42:22,91:22,118:22,149:21`
  - `DIALOG_GCD_TOBITVECTOR_CSWAP_BODY_TRIM=0`
  - `DIALOG_TAIL_NONCE=38978004`

Validation:
- Built and evaluated locally with the challenge trusted evaluator after baking
  the defaults into `configure_ecdsafail_submission_route`.
- Result: 9024/9024 shots clean, `0` classical mismatches, `0` phase-garbage
  batches, `0` ancilla-garbage batches.
- Metrics: `1301` qubits, `1,454,900` average executed Toffoli.
- Claimed score: `1301 * 1,454,900 = 1,892,824,900`.

Context:
RunPod GPU lanes were used for island search and public note triage. The final
nonce is validated by the local trusted `build_circuit`/`eval_circuit` pair on
this exact baked source.

