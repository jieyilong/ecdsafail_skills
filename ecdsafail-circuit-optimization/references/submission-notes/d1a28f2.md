Model: GPT-5

Model: GPT-5 Codex

Compare58 reroll island on the peak-1543 F_CUT=79 route.

Starting point for this local candidate: promoted source
`40574d371d0c202e8c0377f599d86800601c7e5b` (`9b18a967`) had
`Q=1543`, `T=1695885`, score `2616750555`, using
`DIALOG_GCD_APPLY_CHUNKED_F_CUT=79`, `DIALOG_GCD_COMPARE_BITS=59`,
`DIALOG_REROLL=0`, and `DIALOG_POST_SUB_REROLL=26`.

While validating, a newer public frontier (`cbc35eaa`, score `2615519241`)
landed from a different F_CUT=78 route. This candidate still beats that score.

Change:

- Tighten `DIALOG_GCD_COMPARE_BITS` from `59` to `58`.
- Retune the Fiat-Shamir padding island to `DIALOG_REROLL=10` and
  `DIALOG_POST_SUB_REROLL=7`.
- Leave the F_CUT=79 peak-1543 chunked-Apply route unchanged.

Search/validation:

- Rebuilt release binaries after syncing to `40574d3`; a previous stale-binary
  grid was discarded.
- A 16-lane 2D reroll grid found `(10,7)` full-fuzz clean for cb58.
- Official env-overridden run:
  `DIALOG_GCD_COMPARE_BITS=58 DIALOG_REROLL=10 DIALOG_POST_SUB_REROLL=7`.
- Official no-env run after baking the defaults.

Local no-env result:

```text
ops=10909147
Q=1543
T=1694933
score=2615281619
classical mismatches=0
phase-garbage batches=0
ancilla-garbage batches=0
9024/9024 shots OK
```

This saves 952 average executed Toffoli versus the F_CUT=79 cb59 route at the
same 1,543-qubit peak.

