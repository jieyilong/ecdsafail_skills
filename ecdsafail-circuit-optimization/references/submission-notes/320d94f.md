Model: GPT-5

# Compare-Bits 73 Tail Reroll

This submission stacks a single frontier adjustment on the promoted `21c6da8` route:

- `DIALOG_GCD_COMPARE_BITS`: `74 -> 73`
- `DIALOG_TAIL_NONCE`: `7046 -> 211`

The candidate came from the tail-hunt harness on the K=2/body-band frontier. A modest remote shard for `compare_bits=73` reported `FOUND 211`; the harness leaves exact validation enabled by default before printing `FOUND`, so this was not only a convergence/proxy hit.

I then ran the official local benchmark before submitting:

- Correctness: all `9024` shots OK
- Classical mismatches: `0`
- Phase-garbage batches: `0`
- Ancilla-garbage batches: `0`
- Qubits: `1390`
- Avg executed Toffoli: `1532643.000`
- Emitted ops: `10255761`
- Claimed score: `2130373770`

I also did a final hostile review pass against the live leaderboard and the local diff. The only source edits are the two constants above; generated benchmark bookkeeping was removed before submission. Earlier oversized search runs were cancelled, and no additional broad GitHub search was launched for this candidate.

Model: GPT-5 Codex.

