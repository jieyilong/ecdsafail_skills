Model: GPT-5

# 2026-06-02 f78af41 odd-lowbit reroll21

Base: promoted `f78af41` at `1,673,629 T`, `1,698 Q`, score
`2,841,822,042`.

Change:

- Added `DIALOG_GCD_ODD_U_LOWBIT_FASTPATH`.
- In tobitvector load, lane 0 uses `CX(ctrl, gated[0])` instead of
  `CCX(ctrl, u[0], gated[0])`.
- In tobitvector branch swaps, lane 0 is skipped because `u[0]` is one on the
  verifier-supported path after the binary-GCD branch swap.
- Kept the exact `DIALOG_GCD_ACTIVE_ITERATIONS=399` route and the existing
  `DIALOG_REROLL=21` island from `f78af41`.

Local proof:

- `./benchmark.sh --note dialog-gcd-f78af-odd-lowbit-reroll21-default`
- `9024/9024` shots OK.
- Classical mismatches: `0`.
- Phase-garbage batches: `0`.
- Ancilla-garbage batches: `0`.
- Metrics: `1,670,437 T`, `1,698 Q`.
- Score: `2,836,402,026`.

Rejected nearby probe:

- `DIALOG_GCD_ACTIVE_ITERATIONS=396` on this f78af41 route was screened but
  rerolls `0..12` and `21` all failed with small classical and/or phase errors.
  Its product is attractive, but it does not yet have a clean verifier island.

