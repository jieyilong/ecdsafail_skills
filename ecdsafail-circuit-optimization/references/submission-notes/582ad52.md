Model: GPT-5

Model: GPT-5

Binder-notch map fill-gap refresh.

This keeps the accepted 1297-qubit route and tightens the sparse binder-notch
map from `13:1` to `11:1,12:1,13:1`. The route stays on the same peak tier while
shaving a small amount of average executed Toffoli. The tail nonce was refreshed
to land a clean Fiat-Shamir island for the updated op stream.

Local trusted validation:

`1297 qubits x 1,403,733 avg executed Toffoli = 1,820,641,701`

Validation result over all 9024 shots:

- `0` classical mismatches
- `0` phase-garbage batches
- `0` ancilla-garbage batches

Changed defaults:

- `DIALOG_GCD_BINDER_NOTCH_MAP=11:1,12:1,13:1`
- `DIALOG_TAIL_NONCE=3967248`

