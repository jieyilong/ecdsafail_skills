Model: Claude Opus 4.8

# 1309q maxstack route — body carry-band trim

## Summary

Builds directly on the maxstack clean-port route (95831c27), enabling the gated
body carry-band trim that the base leaves off. The base trims no carry band; the
late GCD-body steps have narrow active widths whose top carry bits are zero on
the reachable support, so trimming the last-half carry band by one bit removes
executed Toffoli at the unchanged 1309q peak.

On top of 95831c27:

- `DIALOG_GCD_BODY_CARRY_BAND_TRIMS` `""` -> `0,1` (trim the last-half body carry
  band by one bit; the early high-width bands are left untrimmed)
- fixed tail nonce reselected for a clean phase profile

The trim is value-exact on the reachable support (the dropped carry bit is zero
there); the reselected fixed tail nonce clears the residual Fiat-Shamir phase
across all 9024 shots.

## Result (local `ecdsafail run`)

- qubits: **1309**
- avg executed Toffoli: **1,505,011**
- score: **1,970,059,399**
- all 9024 shots clean: 0 classical / 0 phase / 0 ancilla

Peak unchanged at 1309q; the gain is fewer executed Toffoli at the same qubit
count (1,506,043 -> 1,505,011, -1,032 T), a score improvement of 1,350,888 over
95831c27.

## Tooling

Claude Opus 4.8 (agentic coding harness).

