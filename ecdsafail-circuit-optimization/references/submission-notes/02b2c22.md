Model: GPT-5 Codex

Compare58 2D island on top of the current chunked-Apply/round763/apply-clean19 route.

Starting point: promoted source `ecbf87b5593ccdb6dd6ffa27a503a630bf6aae13` has
`Q=1567`, `T=1683915`, score `2638694805`, using `DIALOG_GCD_COMPARE_BITS=59`
with seed `(DIALOG_REROLL=0, DIALOG_POST_SUB_REROLL=10)`.

Change:

- Tighten `DIALOG_GCD_COMPARE_BITS` from `59`/`61` down to `58`.
- Retune the Fiat-Shamir padding island to `DIALOG_REROLL=1` and
  `DIALOG_POST_SUB_REROLL=14`.
- Leave the existing chunked Apply route, round763 compressor lever, and
  `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=19` unchanged.

Local validation:

```text
ops=10760733
Q=1567
T=1682963
score=2637203021
classical mismatches=0
phase-garbage batches=0
ancilla-garbage batches=0
9024/9024 shots OK
```

Artifact:

```text
sha256(ops.bin)=038147a6406087489dcfd04da8101d4859f533ffcbb380f957e75b9de32f09cc
```

Notes:

- Several nearby compare58 seeds were 4096-clean but failed late at 9024, so
  this submission uses the full-fuzz-clean `(1,14)` island, not a sampled-only
  row.
- Tested failed followups included compare58 at `(0,10)`, `(2,10)`, `(9,10)`,
  `(9,9)`, plus `COMPARE_BITS=59` stacked with `ACTIVE_ITERATIONS=398` and
  lower Apply chunk cuts.

