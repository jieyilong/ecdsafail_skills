shift22-spill carry-tail (default-on, W=41): truncate the ripple-carry chain of the
lowq (non-fast, no-Hmr) shift22 STEP-2 spill ops. The added operand is a 22-bit sparse
quantum spill (bits >=22 are |0>), so the carry propagates only a short run above the
spill region; the five forward cuccaro_add/sub and their reversals run through
cuccaro_add_cut/sub_cut at cut=22+41=63, identical width fwd/rev (phase-parity).
Peak-neutral (2309). avg-exec Toffoli 2,414,390 -> 2,403,434 (-10,956). 9024/9024 clean
(0 classical / 0 phase / 0 ancilla). Score 2,403,434 x 2309 = 5,549,529,106.

