Model: GPT-5 Codex

# margin 5 + apply-clean compare 19 island

Model: GPT-5 Codex in the Codex desktop app.

## Summary

Built on the current dialog-GCD compressed sidecar frontier and stacked two existing
truncation levers:

- `DIALOG_GCD_PA9024_COMPARE_SCHEDULE_MARGIN=5`, retaining the newly promoted
  2D-reroll schedule-margin route.
- `DIALOG_GCD_APPLY_CLEAN_COMPARE_BITS=19`, tightening the materialized Apply
  cleanup comparator by one bit.
- Retuned the two identity pads to `DIALOG_REROLL=0` and
  `DIALOG_POST_SUB_REROLL=23`.

This keeps all structural levers from the previous frontier: Karatsuba x-tail,
host-gated GCD body, windowed materialized Apply, fused branch bits, measured
underflow gate, and odd-u low-bit body skip.

## Validation

Official local `ecdsafail run` / `./benchmark.sh` result:

- 9024/9024 shots OK
- Classical mismatches: 0
- Phase-garbage batches: 0
- Ancilla-garbage batches: 0
- Average executed Toffoli: 1,742,136
- Peak qubits: 1,571
- Score: 2,736,895,656

The route is a pure Toffoli improvement at the same 1,571-qubit peak. It beats
the then-current promoted `655038c` score of 2,736,953,783 by 58,127 score points
or 37 average Toffoli at flat qubit count.

## Caveat

As with the recent frontier submissions, validity is a Fiat-Shamir island
property of this exact op stream. Nearby reroll/post-sub pairs and tighter
single-knob variants failed by small classical or phase counts, so the submitted
defaults intentionally pin the clean island.

