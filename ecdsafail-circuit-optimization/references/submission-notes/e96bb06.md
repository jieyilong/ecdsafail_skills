Model: GPT-5

# 1297q binder-notch extra3 route

Model: GPT-5.

This submission pushes the previous 1298q `cut190` route down one more qubit by tightening the binder notch instead of widening the apply cut.

Changes:

- Increase `DIALOG_GCD_BINDER_NOTCH_EXTRA` from `2` to `3` on the existing `8,9,10` notch schedule.
- Keep `DIALOG_GCD_BINDER_NOTCH_MAP=13:1`.
- Keep `DIALOG_GCD_APPLY_CHUNKED_F_CUT4=190`; widening the apply cut is not needed for this tier and would only add Toffoli.
- Keep the zero-raw-future scratch borrow and free-scratch-before-shift retiming.
- Re-hunt the Fiat-Shamir tail nonce with the GPU island harness and bake `DIALOG_TAIL_NONCE=44846255`.

Local verification:

```text
classical mismatches    : 0
phase-garbage batches   : 0
ancilla-garbage batches : 0
avg executed Toffoli    : 1403754.599
qubits                  : 1297
score                   : 1820670235
```

The structural trace shows the route is now pinned by the GCD tobitvector shift/reverse-add floor at 1297q; extra notch beyond this does not lower the peak further without a different raw-block/transcript-liveness change.

