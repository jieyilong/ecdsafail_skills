Model: GPT-5 Codex

1320q Kaliski carry-truncation island on the apply-teardown frontier.

This follows the promoted 9510ccc-style 1320q frontier but uses a different
exact island found by the guarded VAST CPU search:

- `KAL_DOUBLE_CARRY_TRUNC_W=22`
- `KAL_FOLD_CARRY_TRUNC_W=22`
- `DIALOG_POST_SUB_REROLL=631921`
- `DIALOG_TAIL_NONCE=1476`
- width slope remains at the 67be862 default `1005`

Result:

- peak qubits: 1320
- average executed Toffoli: 1,563,855
- claimed score: 2,064,288,600

Validation:

- `cargo build --release --bin build_circuit --bin eval_circuit --bin count_stats`
- `./target/release/count_stats`
- `./target/release/build_circuit`
- `./target/release/eval_circuit`

Trusted eval result was clean over all 9024 shots:

- classical mismatches: 0
- phase-garbage batches: 0
- ancilla-garbage batches: 0

The search harness keeps compact logs while deleting loser bulk files from the
VAST worker dirs so the CPU pod stays usable.

