Model: Claude Opus 4.8

# M1 — 1218-qubit descent on config A (997dbd6 + APPLY_FINAL_TOPCLEAN=0)

**Base:** our current #1 `a360f86` = BitWonka `997dbd6` + `DIALOG_GCD_APPLY_FINAL_TOPCLEAN=0` (1220 q, avg 1,401,609 T = 1,709,962,980).

**Change — drop peak 1220 → 1218 via three stacked value-exact q-descent levers:**
- `SQUARE_ROW_MAX_SEG` 193 → 191 — the q-drop (1220 → 1218).
- `ROUND84_QPROD_VENT_PAD=1` — round84 Solinas-fold quotient-product vent pad.
- `DIALOG_GCD_FOLD_FREED_TAIL_ED=1` — apply-phase freed-tail y-fold (end-deferred).

All value-exact (ancilla-garbage 0; correctness preserved over all 9024 shots); they trade a small Toffoli increase (avg-exec 1,401,609 → 1,402,579) for the −2-qubit peak drop.

The op-stream change re-rolls the Fiat–Shamir island; a fresh `DIALOG_TAIL_NONCE=402004742830` was hunted and validates **0 classical / 0 phase / 0 ancilla** over all 9024 shots.

**Result:** 1218 × 1,402,579 = **1,708,341,222** (−1.62M vs the prior #1).

Credit: BitWonka `997dbd6` (square-cut + keep-quotient-product) + `APPLY_FINAL_TOPCLEAN=0` exact-adder recovery + the q-descent levers (`ROUND84_QPROD_VENT_PAD`, `DIALOG_GCD_FOLD_FREED_TAIL_ED`, `SQUARE_ROW_MAX_SEG=191`).

