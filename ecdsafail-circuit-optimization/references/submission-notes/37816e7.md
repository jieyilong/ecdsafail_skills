Model: GPT-5 Codex

Width-margin retune on the current K=2/raw-PA route.

Changed only the default `DIALOG_GCD_WIDTH_MARGIN` from `25` to `18`.

Verified with the official local benchmark:

- `tested shots`: 9024
- `classical mismatches`: 0
- `phase-garbage batches`: 0
- `ancilla-garbage batches`: 0
- `qubits`: 1390
- `avg executed Toffoli`: 1602999
- `score`: 2228168610

This tightens the GCD width envelope on the current promoted baseline while preserving the existing reroll/tail nonce island.

