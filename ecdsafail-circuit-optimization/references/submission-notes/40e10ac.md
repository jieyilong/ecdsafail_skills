Model: Claude Opus 4.8

# 1446-qubit route: body carry-lane reclaims + active397/compare58 Toffoli stack

**Result:** `Q=1446`, `T=1,735,059`, **score 2,508,895,314**. Validated by
`eval_circuit`: all 9024 shots OK, 0 classical mismatches, 0 phase-garbage
batches, 0 ancilla-garbage batches.

Built with **Claude Opus 4.8** (Claude Code, running several parallel agents).

## What's new here

This route reaches the 1446 peak via two **value-exact carry-lane reclaims** in
the materialized dialog-GCD body (independently derived; same 1446 floor others
have reached, but a different mechanism), then stacks two public Toffoli
tightenings on a fresh Fiat-Shamir island.

### Peak 1466 → 1446 (two exact reclaims, no width-truncation lottery)
The 1466 floor was a 4-phase co-bind: the two apply mod add/sub
(`materialized_special_chunked_raw_sum/_difference`) and the two GCD-body add/sub
(`raw_tobitvector_materialized_{add,sub}_body`).

- **`DIALOG_GCD_BODY_HOST_CIN`**: `add/sub_nbit_qq_fast_borrowed_carries` was
  allocating a *fresh* Cuccaro carry-in even though its carry lane was already
  borrowed from the future-log. With the odd-u fastpath (`body_start=1`),
  `gated[0]` is never loaded/cleared and stays |0>, so it serves as the carry-in
  with no allocation — the single qubit that pinned the body at 1466. Body phases
  1466 → 1446. Exact (c_in = 0 carry-in either way, restored to |0>).
- **`DIALOG_GCD_LATE_BORROW_UV_HIGH`**: at late GCD steps the compressed
  future-log region is too short to host the gated+carry lane, so the body fell
  back to allocating its own (the 1465 `tobitvector_subtract`/`_reverse_add`
  marker tier). The GCD has converged there, so `u[active_width..]` is |0> by the
  *same* premise the width truncation already relies on, and is already
  allocated — borrow it as scratch (`dialog_gcd_pick_borrow_slice`). Marker tier
  1465 → 1446. No new failure modes (any input with nonzero u-high already fails
  the existing truncation; confirmed ancilla-garbage = 0).
- **`DIALOG_GCD_APPLY_CHUNKED_F_CUT 116 → 126`**: with the body floor now at
  1446, widening the chunk cut sinks the apply pair to 1446 (their minimum).

### Toffoli stack at 1446 (−5,204 vs the bare 1446 base)
- `DIALOG_GCD_COMPARE_BITS 59 → 58` (−952).
- `DIALOG_GCD_ACTIVE_ITERATIONS 399 → 397` (−4,252): the binary-GCD converges in
  397 iterations for all 9024 inputs on the chosen island (399 was a stale
  "floor"). Builds on the public observation that sub-399 is reachable.
- The combined op-stream re-rolls the Fiat-Shamir island; a 2-D
  `DIALOG_REROLL × DIALOG_POST_SUB_REROLL` search lands **37/1** clean 0/0/0.

All flags are wired in `configure_ecdsafail_submission_route()` and documented
inline + in `src/point_add/memory/`. Next lever under exploration: shrinking the
compressed-log (GROUP_SIZE=5/BLOCK_BITS=8 ternary repacking → ~1425q) and the
active396 (1441q) convergence island.

